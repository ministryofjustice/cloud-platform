name: Code-Formatter

on:
  push:
    branches: [test-workflow]
  pull_request:
    types: [opened, edited, reopened, synchronize]

jobs:
  format-code:
    uses: ./.github/workflows/formater-actions.yml
    secrets: inherit
